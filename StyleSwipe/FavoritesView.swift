import SwiftUI

struct FavoritesView: View {
    var body: some View {
        VStack {
            Text("My Favorites")
                .font(.largeTitle)
                .fontWeight(.bold)
                .padding()
            
            Spacer()  // Spacer to push the text to the top
        }
        
    }
}
